<button @onclick="OnClick" type="@Type"
    class="w-full mt-4 py-3 bg-white dark:bg-gray-900 text-black dark:text-white border-2 border-black rounded-lg font-semibold shadow transition-all duration-150 flex items-center justify-center gap-2 hover:bg-gray-50 dark:hover:bg-gray-800 hover:-translate-y-0.5 hover:shadow-lg active:scale-95 @Class"
    disabled="@Disabled">
    @if (!string.IsNullOrEmpty(Icon))
    {
        <i class="@Icon"></i>
    }
    @ChildContent
</button>

@code {
    [Parameter] public string Icon { get; set; }
    [Parameter] public string Type { get; set; } = "button";
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public string Class { get; set; }
    [Parameter] public EventCallback<MouseEventArgs> OnClick { get; set; }
    [Parameter] public RenderFragment ChildContent { get; set; }
}